# Implementation Plans

Generated by the improve skill (001 on 2026-08-08; 002–009 on 2026-09-10, planned at commit
`26270fd`). Execute in the order below unless dependencies say otherwise. Read each plan fully,
honor its STOP conditions, and update its status when done.

Note for 002–009: the working tree carried uncommitted changes when these were planned
(`Budget.kt`, `BudgetUi.kt`, `Daily.kt`, `Derived.kt`, `MainActivity.kt`, `Report.kt`,
`Reports.kt`, `Ui.kt`, plus two test files). Each plan's drift check is excerpt-based for that
reason, and plans deliberately avoid touching those files — 009 has a hard clean-tree
precondition.

## Execution order and status

| Plan | Title | Priority | Effort | Depends on | Status |
|---|---|---|---|---|---|
| 001 | Make monthly cash flow a first-class report | P1 | L | none | DONE — device QA ran on the `mt37clean` emulator, not a physical phone |
| 002 | Pin the rates Worker's upstream adapters with fixture tests | P1 | M | none | DONE — reviewed 2026-09-10; 7 hermetic fixture tests, worker gate re-run green (47/47) |
| 003 | Keep transaction details off the lock screen | P1 | S | none | DONE — reviewed 2026-09-10; NotifyTest green on merged tree; nit: «الفظ» in the new KDoc should read «لفظ» (plan-author typo, one character, fix on commit) |
| 004 | Pin pairing links to the configured sync origin | P1 | S | none | DONE — reviewed 2026-09-10; 8-case origin matrix green, neighbouring sync suites re-run green |
| 005 | Test the Android sync client's household lifecycle | P2 | M | 004 | DONE — reviewed 2026-09-10; 9 lifecycle tests green on merged tree, asserting both HTTP conversation and durable state |
| 006 | Cache npm in CI and stop building the PWA twice | P2 | S | none | DONE — reviewed 2026-09-10; action SHAs verified against upstream tags; final confirmation is the next CI run |
| 007 | Write AGENTS.md — the operating contract for coding agents | P2 | S | none | DONE — reviewed 2026-09-10; 73 lines, spot-checked commands ran (worker typecheck, MoneyTest) |
| 008 | A small instrumented UI slice for the flows JVM tests cannot see | P3 | L | none | DONE — reviewed 2026-09-10; compiles for both editions, all 22 semantic anchors verified against production code; suite has NOT yet run on a device — first execution is the `ui-check` workflow (trigger it once after pushing) |
| 009 | Split Ui.kt and MainActivity.kt along their existing seams | P3 | L | none (hard clean-tree precondition) | BLOCKED — Ui.kt/MainActivity.kt (and six more files) carry uncommitted local changes; commit or stash them, then dispatch |

Status values: TODO, IN PROGRESS, DONE, BLOCKED with a one-line reason, or REJECTED with a one-line
rationale.

## Dependency notes

- Plan 001 is self-contained.
- Plan 005 requires plan 004: its tests use the `allowedBase` parameter 004 adds to
  `joinHousehold`/`rejoinHousehold`/`pairHousehold`.
- Plans 002/003/004/006/007/008 touch pairwise-disjoint files and can run in parallel.
- Plan 009 must wait for the operator to commit or stash the uncommitted changes listed above,
  and should land last regardless — it moves code the other plans' reviews may still be reading.

## Findings considered and rejected

From the 2026-08-08 run:

- Weekly/monthly/yearly range selection: deferred because only the monthly question is currently
  grounded in the product request and complete enough in the ledger model.
- Historical SMS backfill: deferred because it changes onboarding workload, privacy expectations,
  and data coverage rather than only report presentation.
- A separate cash-flow root tab: rejected because Reports is already a root and nested report modes
  solve the hierarchy without expanding the five-item navigation bar.
- Copying the reference app's purple/gold palette: rejected because this app already has a coherent
  theme and semantic color roles; only the monthly comparison pattern is relevant.

From the 2026-09-10 audit (vetted against the code; do not re-report):

- "PWA pairing is broken — `/v1/pair` requires a device token": wrong; the all-zero placeholder
  secret passes `splitToken`'s syntactic gate deliberately, and the Durable Object's `pair`
  authorises by one-time code (`sync/src/index.ts:394`), covered by `sync/test/sync.test.ts`.
- "Scope key persists in browser history": already mitigated — `pwa/src/main.ts:513-516` strips
  the fragment via `history.replaceState`; the residual (screenshot of the QR itself) is a
  documented ceiling in `pwa/src/crypto.ts`.
- `!!` clusters at money/persistence boundaries (`Derived.kt:763`, `Links.kt:280`,
  `Ledger.kt:783`, `MainActivity.kt:412`, `Daily.kt:59`): every cited site is locally guarded
  (groupBy non-empty, emptiness `continue`, `clockRunsAhead` null-checks, wallet-filtered lists);
  13 occurrences in 29k lines is discipline, not debt.
- `allBalances` N+1 (`Derived.kt:759`): real shape, but N is a household's handful of accounts on
  local SQLite — measured impact too small to justify fix risk.
- PWA `decisionsFor` per-target reads (`pwa/src/db.ts:224`): all `getAll`s share one IndexedDB
  transaction on an indexed key; not worth restructuring.
- `buildCashFlow` multi-pass over entries (`Reports.kt:825`): sits behind `remember(...)` keyed on
  its inputs (`Ui.kt:803`), recomputes only on state change; in-memory passes over a year's
  entries are single-digit ms — not worth the risk on money-display code. Revisit only with a
  measured jank trace.
- Dependency lag: none found — catalogs current, Dependabot queue is routine bumps gated by CI.
- TS formatter/linter adoption: optional churn; the three packages are hand-formatted
  consistently. AGENTS.md (plan 007) records the convention instead.
